<h1>Vehicle List</h1>
<% @vehicles.each do |vehicle| %>
		<div class="container">
			<h5><%= vehicle.year %> <%= vehicle.make %> <%= vehicle.model %>
				 <%= link_to "More", vehicle_path(vehicle), :class => 'btn btn-secondary'%></h5>
		</div>
	<% end %>